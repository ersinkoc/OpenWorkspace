# Example: Basic Gmail

Search and read Gmail messages using the `@openworkspace/gmail` package.

## Usage

```bash
npx tsx index.ts
```

## What it demonstrates

- Creating an authenticated HTTP client
- Searching Gmail messages with query filters
- Fetching full message details by ID
- Handling the `Result<T, E>` pattern for error-safe operations
