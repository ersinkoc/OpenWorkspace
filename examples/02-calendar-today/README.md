# Example: Calendar Today

List today's calendar events using the `@openworkspace/calendar` package.

## Usage

```bash
npx tsx index.ts
```

## What it demonstrates

- Querying calendar events within a specific time range
- Using `singleEvents: true` to expand recurring events
- Sorting events by start time
- Formatting event times for display
