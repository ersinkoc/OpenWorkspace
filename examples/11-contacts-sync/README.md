# Example: Contacts Sync

Sync contacts between two Google accounts using the `@openworkspace/contacts` package.

## Usage

```bash
npx tsx index.ts
```

## What it demonstrates

- Listing contacts from a source account
- Searching for existing contacts in a target account
- Creating new contacts that do not yet exist in the target
- Using parallel HTTP clients for multi-account operations
