# Example: Interactive Send

Compose and send an email interactively, including HTML content and attachments.

## Usage

```bash
npx tsx index.ts
```

## What it demonstrates

- Composing an email with plain text and HTML body
- Adding file attachments to an outgoing message
- Using the `sendMessage` function with `SendOptions`
- Creating a draft before sending for review
