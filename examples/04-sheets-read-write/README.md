# Example: Sheets Read and Write

Read and write data to Google Sheets using the `@openworkspace/sheets` package.

## Usage

```bash
npx tsx index.ts
```

## What it demonstrates

- Reading cell values from a spreadsheet range
- Writing a 2D array of values to a range
- Appending rows to an existing sheet
- Fetching spreadsheet metadata
