# Example: Drive Upload

Upload and download files from Google Drive using the `@openworkspace/drive` package.

## Usage

```bash
npx tsx index.ts
```

## What it demonstrates

- Uploading a text file to Google Drive
- Creating a folder and uploading into it
- Downloading a file by its ID
- Using the `Result` pattern for each operation
